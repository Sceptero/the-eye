// Karma module, test user messages for "good" or "evil" deeds and keeps track of their karma
"use strict";

module.exports = class Karma {
    constructor(bot) {
        this.bot = bot;
    }
    
    
    checkGoodDeed(msg) {
        //todo
    }

    checkEvilDeed(msg) {
        //todo
    }
}